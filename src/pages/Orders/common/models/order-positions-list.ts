import { PositionAttrError } from "../interfaces/position-errors";

export const usePositionsLists = (
  clickHandler?: (position_id: string) => void,
  errors?: Map<string, PositionAttrError> | null,
) => {};
