export interface AppHidePanelBtnComponent {
  open: boolean;
  onClick: (open: boolean) => void;
}

export interface AppLogoComponent {
  minimized: boolean;
}

export interface AppMenuComponent {
  minimized: boolean;
}

export interface AppUserInfoComponent {
  minimized: boolean;
}
